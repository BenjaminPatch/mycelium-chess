import React from 'react';
import Button from '@material-ui/core/Button';

const GaugeEngagement: React.FC = () => {
  return (
    <Button variant="text" color="primary">
      Gauge Engagement
    </Button>
  );
}

export default GaugeEngagement;